/target/
/.embuild/
Cargo.lock

# Per-session device captures via flash-monitor.sh / cat /dev/ttyACM0.
/logs/

# Build-time WiFi credentials and UDP log target. Created locally by
# the developer per `cfg-sample.toml`; never committed.
cfg.toml
cfg.toml-*

# build.rs / bootstrap-sdkconfig.sh が CARGO_MANIFEST_DIR の絶対パスで
# CONFIG_PARTITION_TABLE_CUSTOM_FILENAME を埋め込む生成ファイル。
sdkconfig.gen.defaults
