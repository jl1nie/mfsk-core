# mfsk-core — ハムフェア2026 資料テキスト

このファイルが**文言の正本**です。ここを直して `python3 build.py build` を走らせると、
HTML と PDF の両方が作り直されます。レイアウト（判型・段組・色・表の列幅）は
HTML 側が持っているので、このファイルでは文言だけを扱います。

## 編集のしかた

- `### F02-05 · 本文` のような見出しの**次の行から次の見出しまで**が、その箇所の文言です。
  見出しの行（ID とラベル）は**変えないでください** — 差し込み先を見失います。
- 装飾は次の記法で書けます。
  `**太字**` / `` `等幅` `` / `==マーカー==` / `++アクセント色++` / `^小さめ^`（数値の単位）
- **改行したいところで改行**してください。そのまま改行として出ます。
  表と箇条書きの中だけは、行が壊れるので改行に `<br>` を使ってください。
- 表は Markdown の表のまま編集できます。行の増減も可能です（列は増やせません）。
- 箇条書きは `- ` で始まる行です。項目の増減も可能です。
- ここに書いていない HTML タグはそのまま素通しします。分からないものは触らないのが安全です。

## 直したあと

- 文言を増やすとページからはみ出すことがあります。`build` が各ページの超過量を報告するので、
  `over` が 0 でない行が出たら、その分だけ削るか、HTML 側の `class="page dense"` で寸法を落とします。
- 数字を直すときは `README.md` の出典表も一緒に確認してください。

---

# フリップ資料（A4横10ページ・ブースでめくる用）

## F01 — 表紙

### F01-01 · キッカー

HAM FAIR 2026 — BOOTH

### F01-02 · 大見出し

mfsk-core

### F01-03 · URL行

github.com/jl1nie/mfsk-core &nbsp;·&nbsp; crates.io/crates/mfsk-core &nbsp;·&nbsp; GPL-3.0-or-later

### F01-04 · キャッチ

WSJT-X のデコーダを、++まるごと Rust で++書き直しました。
M5Stack CoreS3でリアルタイムでFT8が動きます。

### F01-05 · モード一覧

**FT8 · FT4 · FST4 · WSPR · JT9 · JT65 · Q65 · MSK144**
8つのモードファミリを、ひとつのライブラリで。デスクトップにも、ブラウザにも、スマホにも、マイコンにも。

### F01-06 · 要点の見出し

WSJT-X と同等

### F01-07 · 要点の説明

実録音での再現率も、AWGN 感度も、
実バイナリと突き合わせて確認しています

### F01-08 · 要点の見出し

1つのソース

### F01-09 · 要点の説明

デスクトップ / WASM / Android・iOS /
no_std マイコン — 同じコードが動きます

### F01-10 · 要点の見出し

約150 KB で動く

### F01-11 · 要点の説明

FT8 の復調に必要な RAM。
実機で、実際に電波を復調しています

### F01-12 · 署名

<span class="u">JL1NIE</span> / 友部 実
<span class="note" style="font-size:10.5pt">デモ機：M5Stack CoreS3 + IC-705（USB Audio 直結、PC なし）</span>

### F01-13 · QRの説明

github.com/jl1nie/mfsk-core

## F02 — デモ：いま目の前で起きていること

### F02-01 · 見出し

デモ：組み込み機器で動作するFT8デコーダ

### F02-02 · 右肩の補足

M5Stack CoreS3

### F02-03 · 図・主文

IC-705

### F02-04 · 図・副文

40 m / USB Audio Class
受信音声 48 kHz

### F02-05 · 図・矢印ラベル

USB-C
1本

### F02-06 · 図・主文

M5Stack CoreS3

### F02-07 · 図・副文

ESP32-S3 LX7 240 MHz 2コア
8 MB PSRAM / 16 MB Flash

### F02-08 · 図・矢印ラベル

約1.2秒
スロット後

### F02-09 · 図・主文

LCD にコールサイン

### F02-10 · 図・副文

タッチパネルでモード切替
WiFi / NTP / HTTP 設定

### F02-11 · 図の注記

デコードはすべて CoreS3 の中で完結しています。

### F02-12 · 小見出し

実測（2026-08-23、IC-705 実機 40 m）

### F02-13 · 数値

6–8^ 局^

### F02-14 · 数値の説明

1スロット（15秒）あたりの
FT8 デコード数

### F02-15 · 数値

−24^ dB^

### F02-16 · 数値の説明

到達した最弱信号
（+8 〜 −24 dB）

### F02-17 · 注記

同日、WSPR も `slot 1 src=uac decoded 1 station(s)` を記録。 USB Audio 取り込み自体は 10分間・125 MB 連続無エラー、WiFi 接続を維持したまま。

### F02-18 · 小見出し

1つのイメージに、4つの受信機

### F02-19 · モード名

uac

### F02-20 · モードの説明

無線機の USB Audio から FT8

### F02-21 · モード名

wspr

### F02-22 · モードの説明

WSPR 受信・スポット一覧

### F02-23 · モード名

fst4

### F02-24 · モードの説明

FST4 ワイドバンド監視

### F02-25 · モード名

decode

### F02-26 · モードの説明

内蔵 WAV のデモ
（無線機不要）

### F02-27 · 本文

どれを動かすかは NVS に保存され、==タッチパネルで切り替わります==。 書き換え不要 — USB ホストを起動するとフラッシャが使うポートを奪ってしまうので、これは仕様上どうしても必要でした。

### F02-28 · 注記

**電源のきまり：**USB-C は「もらう」と「配る」を同時にできません。起動時に VBUS があれば ペリフェラル（充電・書き込み・シリアル）、なければ USB ホスト（無線機を鳴らす）。役割は起動の瞬間に決まります。

### F02-29 · まとめ帯

WSJT-X と同じアルゴリズムが、手のひらの ESP32 の中で、無線機から直接受けた音を復調しています。

### F02-30 · フッタ

<span>mfsk-core — ハムフェア2026 ブース資料</span><span>02 / 10</span>

## F03 — なぜ書き直したのか

### F03-01 · 見出し

なぜ書き直したのか

### F03-02 · 右肩の補足

WSJT-X は基準実装であり続けます

### F03-03 · 小見出し

WSJT-X が素晴らしいこと

### F03-04 · 箇条書き

- K1JT Joe Taylor 氏らによる、実戦で鍛えられた実装
- 本プロジェクトのプロトコル定数は、すべてここが出典
- デスクトップでは徹底的に最適化されている
- **基準実装の座は、これからも WSJT-X のもの**

### F03-05 · 小見出し

それでも困ること

### F03-06 · 箇条書き

- Fortran + C + Qt の混成デスクトップアプリ
- ブラウザ（WASM）には載らない
- Android / iOS で Fortran ランタイムを積むのは非現実的
- マイコン（no_std）はそもそも土俵の外

### F03-07 · テキスト

▼

### F03-08 · 強調ボックス

アルゴリズムはそのまま。<span style="color:var(--accent)">載る場所を増やす。</span>

### F03-09 · 小見出し

1:1 の移植

### F03-10 · 本文

全アルゴリズムファイルが、移植元の `lib/*.f90` / `lib/*.c` を明記しています。 意図的に WSJT-X と変えた箇所は、必ずコメントでそう書く決まりです。

### F03-11 · 小見出し

再現性で守る

### F03-12 · 本文

WSJT-X 配布の実録音に対する golden テストと、AWGN 掃引による感度測定。 **再現率（recall）だけでなく誤デコード（precision）も**常に測ります。

### F03-13 · 小見出し

依存を持たない

### F03-14 · 本文

純 Rust。FFT すら外部から差し替え可能。 `no_std + alloc` でビルドでき、C からも呼べる ABI を用意しています。

### F03-15 · まとめ帯

基準実装を置き換えるのではなく、基準実装が行けない場所へ持っていくためのライブラリです。

### F03-16 · フッタ

<span>mfsk-core — ハムフェア2026 ブース資料</span><span>03 / 10</span>

## F04 — 対応モード

### F04-01 · 見出し

対応モード

### F04-02 · 右肩の補足

8ファミリ / レジストリ上 16 の実装

### F04-03 · 表

| モード | スロット | 誤り訂正 | メッセージ | 同期 | feature |
|---|---|---|---|---|---|
| **FT8** | 15 s | LDPC(174, 91) + CRC-14 | 77 bit | Costas-7 × 3 | `ft8` |
| **FT4** | 7.5 s | LDPC(174, 91) + CRC-14 | 77 bit | Costas-4 × 4 | `ft4` |
| **FST4** -15/-30/-60A/-120/-300 | 15–300 s | LDPC(240, 101) + CRC-24 | 77 bit | Costas-8 × 5 | `fst4` |
| **WSPR** | 120 s | 畳み込み r=½ K=32 + Fano | 50 bit | シンボル毎 LSB (npr3) | `wspr` |
| **JT9** | 60 s | 畳み込み r=½ K=32 + Fano | 72 bit | 分散 16 スロット | `jt9` |
| **JT65** | 60 s | Reed-Solomon(63, 12) GF(2⁶) | 72 bit | 分散 63 スロット | `jt65` |
| **Q65** -30A / -60A‥E | 30 / 60 s | QRA(15, 65) GF(2⁶) + CRC-12 | 77 bit | 分散 22 スロット | `q65` |
| **MSK144** | 15 s | LDPC(128, 90) + CRC-13 | 77 bit | 流星バースト走査（整合フィルタ） | `msk144` |

### F04-04 · 小見出し

デコードもエンコードも

### F04-05 · 本文

各モードとも、復調（デコード）だけでなく変調波の生成（シンセサイズ）も実装しています。 テストは「自分で作った波形を自分で復調する」だけでは信用しない方針で、 **WSJT-X が配布している実際の録音**に対する既知の正解デコードと突き合わせます。

### F04-06 · 小見出し

MSK144 だけは別扱い

### F04-07 · 本文

MSK144 は連続位相の2値 MSK で、他モードのような「固定スロット」の枠に収まりません。 そのため共通の `Protocol` トレイトには載せず、専用の復調器を持ちます。 これは埋め残しではなく、設計上の判断です。

### F04-08 · 注記

FST4 は5つの T/R 周期サブモード、Q65 は 30 秒 1 + 60 秒 EME 用 5 のサブモードを持ちます。 同じ FEC・メッセージ符号・同期配置を共有し、NSPS とトーン間隔だけが異なります。

### F04-09 · まとめ帯

8つのモードファミリ。デコードもエンコードも、すべて同じ1つのライブラリの中にあります。

### F04-10 · フッタ

<span>mfsk-core — ハムフェア2026 ブース資料</span><span>04 / 10</span>

## F05 — 感度：WSJT-X と同等、あるいはそれ以上

### F05-01 · 見出し

感度：WSJT-X と同等、あるいはそれ以上

### F05-02 · 右肩の補足

docs/notes/BENCHMARKS.md

### F05-03 · 表

| モード | 実録音での再現率 | 誤デコード | AWGN 感度の WSJT-X 比 |
|---|---|---|---|
| **FT8**（ホスト） | 8/8（WSJT-X）, 18/18（JTDX） | **裏付けのない検出 0** | 約 −21.6 dB（WSJT-X は −20〜−21 dB） |
| **FT4** | 6/6 | **0** | 約 −16.9 dB — 同一コーパスで実バイナリ `jt9 -5` と一致〜わずかに優位 |
| **FST4** | 1/1（FST4-60A） | **0** | 実バイナリ `jt9 -7` と 3サブモード中 2つで一致（FST4-60 は完全一致） |
| **WSPR** | 9/9 | **0**（5スロット連鎖でも 0） | 50% 交差 約 −31.5 dB — 実 `wsprd` とセル単位で一致 |
| **JT9** | 7/7 | **0** | 50% 交差 約 −26.6 dB — 実 `jt9 -9` の既定深度を上回る |
| **JT65** | 参照録音なし | **0** | 自前コーパスでは差ゼロ。ただし実バイナリ `jt9 -6` にはまだ差がある（未解決） |
| **Q65** | 実 EME 録音 2件 | サブモード毎に管理 | 10 サブモードで解析値の 0.2〜1.4 dB 以内。CQ-AP ありでは WSJT-X の判定に一致〜優位 |
| **MSK144** | 3/3（SNR 値も完全一致） | **0** | 50% 交差 約 −5.2〜−5.8 dB — 実 `jt9` と 28セル中 25セルが完全一致 |

### F05-04 · 小見出し

「再現率だけ」はベンチマークではない

### F05-05 · 本文

2026-08-11 まで、この表には再現率しか載っていませんでした。 当時「WSPR 8/8、パリティ達成」と書かれていたデコーダは、 ==同時に1スロットあたり8個の存在しないコールサインを出していました== — 誤検出率 50%。 再現率だけの表では、それを正常なデコーダと区別できません。 いまは precision が列として常設されています。

### F05-06 · 小見出し

数字は「観測」ではなく「表明」

### F05-07 · 箇条書き

- 各モードの `*_wsjtx_samples` テストが、上の数字を **assert しています**。下回れば CI が落ちます。
- `MFSK_REQUIRE_CORPUS=1` — 録音が見つからないときは「スキップ」ではなく「失敗」。静かに緑になる事故を潰すため。
- 「余分なデコード」は必ずしも誤りではありません。FT8 の混雑バンドでは WSJT-X の 8 に対し 20 出ますが、 **超過分 12 はすべて JTDX の正解表に載っています** — 発明ではなく、WSJT-X が取り逃した信号です。

### F05-08 · まとめ帯

速いことより、正しいこと。取り逃さないことと、でっち上げないことを、同時に測っています。

### F05-09 · フッタ

<span>mfsk-core — ハムフェア2026 ブース資料</span><span>05 / 10</span>

## F06 — 速度

### F06-01 · 見出し

速度

### F06-02 · 右肩の補足

同じアルゴリズムが、Ryzen でもマイコンでも

### F06-03 · 小見出し

ホスト — 実録音 1スロットの復調時間

### F06-04 · 表

| モード | スロット長 | 復調時間 |
|---|---|---|
| Q65-60D（10 GHz EME） | 60 s | 0.05 s |
| FT8（組み込み構成） | 15 s | 0.07 s |
| FST4-60A | 60 s | 0.08 s |
| FT4 | 7.5 s | 0.10 s |
| JT9 | 60 s | 0.16 s |
| FT8（ホスト全力構成） | 15 s | 0.18 s |
| WSPR | 120 s | 0.76 s |
| MSK144 | 15 s | 0.88 s |

### F06-05 · 注記

AMD Ryzen 9 9900X (12C/24T) / `--release --features full`。 WSPR が他より遅いのは意図的で、`wsprd` の DT ピーク探索を入れて約 1 dB の感度と引き換えにしています。

### F06-06 · 小見出し

組み込み — 同じ整数パイプライン

### F06-07 · 表

| 実行環境 | 復調時間 |
|---|---|
| Ryzen ホスト（固定小数点） | 約 6 ms |
| **ESP32-S3 LX7 実機** | **1.19 s** |
| ESP32 Core2 LX6 実機 | 約 2.8 s |

### F06-08 · 注記

16局が混み合う WSJT-X 参照録音 `qso3_busy.wav`、スロット終了後の実測。 ホストとマイコンの差はほぼ純粋な CPU 比（Ryzen 約5 GHz×16コア 対 Xtensa 240 MHz×2コア）で、 **アルゴリズム上のオーバーヘッドはありません** — 両者は同一の整数パイプラインです。

### F06-09 · 小見出し

WSPR をマイコンに載せる：1214秒 → 101.6秒

### F06-10 · 本文

120秒スロットに対し最初は **10.1倍の超過**。 独立した8つの改善を積み上げて **スロットの 0.85倍**に収め、9/9 の正解デコードを全段階で維持しました。 効いたのは新アルゴリズムではなく、==参照実装 `wsprd` が既に持っていた候補フィルタ==（`minsync2`、単独で 4.1倍）。 しかも副産物として誤デコードが 0 になりました。

### F06-11 · 注記

**FT8 は2つの構成を持ち、決して混ぜません。** ホスト構成（`DecodeDepth::FULL`）は研究用の全力設定。組み込み構成（`EMBEDDED`）は ESP32 の時間予算に合わせて意図的に軽くした別のデコーダで、劣化版ではありません。 FT8 の QSO 折り返し予算はスロット終了後 **約2秒** — その中で復調・ウォーターフォール描画・次スロットの送信準備まで済ませる必要があります。

### F06-12 · まとめ帯

Ryzen で 6 ミリ秒、ESP32-S3 で 1.19 秒 — 差は純粋な CPU 性能比です。走っているコードは同じものです。

### F06-13 · フッタ

<span>mfsk-core — ハムフェア2026 ブース資料</span><span>06 / 10</span>

## F07 — マルチプラットフォーム

### F07-01 · 見出し

マルチプラットフォーム

### F07-02 · 右肩の補足

ソースは1つ。feature で行き先を変えるだけ

### F07-03 · 表

| 行き先 | ターゲット | FFT / 並列化 | 状態と備考 |
|---|---|---|---|
| **デスクトップ / サーバ** | x86_64, aarch64 | rustfft + rayon | 既定構成。crates.io から `cargo add mfsk-core` ですぐ |
| **ブラウザ** | `wasm32-unknown-unknown` | rustfft の WASM SIMD | `+simd128` を明示するのを忘れずに（既定では付きません） |
| **Android / iOS** | NDK arm64-v8a ほか | NEON | C ABI（`mfsk-ffi`）経由。Fortran ランタイム不要 |
| **マイコン** | `no_std + alloc`<br>ESP32-S3 / RP2350 / Cortex-M | 呼び出し側が注入<br>（esp-dsp / CMSIS-DSP） | 実運用中。`fft-extern` + `fixed-point` |
| **C / C++ から** | staticlib | — | `mfsk-ffi`（全モード）と `mfsk-ffi-ft8`（FT8 のみ・組み込み向け）。<br>ヘッダは cbindgen 生成、C++ 実ドライバを CI が毎回動かしています |

### F07-04 · 小見出し

数値型を差し替える

### F07-05 · 本文

DSP と FEC のパイプライン全体が**スカラ型トレイト**で抽象化されています。 ホストは f32、マイコンは u16 スペクトログラム + i16 DFT + Q11i16 LLR + 整数 BP。 ==コードは同じ一本==です。

### F07-06 · 小見出し

FFT を差し替える

### F07-07 · 本文

FFT はトレイト越しの外部注入。 ホストは rustfft、ESP32 は esp-dsp のアセンブラ、Cortex-M は CMSIS-DSP — ライブラリ側は「誰が計算したか」を知りません。

### F07-08 · 小見出し

使うモードだけ積む

### F07-09 · 本文

モードは全部 feature フラグ。FT8 だけ欲しければ FT8 だけがバイナリに入ります。 **12通りの feature 組み合わせ**を CI が毎回ビルドして、 「full でしかビルドが通らない」事故を防いでいます。

### F07-10 · まとめ帯

行き先が変わっても書き直しは起きません。変わるのは feature フラグと、外から渡す FFT だけです。

### F07-11 · フッタ

<span>mfsk-core — ハムフェア2026 ブース資料</span><span>07 / 10</span>

## F08 — 組み込み対応の中身

### F08-01 · 見出し

組み込み対応の中身

### F08-02 · 右肩の補足

docs/reference/EMBEDDED.md（日本語版あり）

### F08-03 · 数値

約150^ KB^

### F08-04 · 数値の説明

FT8 復調に必要な使用可能 RAM
（FFT を呼び出し側が用意する場合）

### F08-05 · 数値

150–200^ KB^

### F08-06 · 数値の説明

ライブラリ分のフラッシュ使用量
（Core2 実測、アプリのグルー込み）

### F08-07 · 数値

0^ バイト^

### F08-08 · 数値の説明

内部 DRAM の常駐スクラッチ
／要求する extern シンボル

### F08-09 · 小見出し

ライブラリが呼び出し側に求めるもの

### F08-10 · 箇条書き

- **FFT の実装ひとつ** — `fft-extern` でファクトリ関数を1つ提供するだけ。ESP32 なら esp-dsp をそのまま使えます。
- **アロケータ** — `no_std + alloc`。スロット単位の作業領域は PSRAM で構いません。
- それ以外は何もありません。**スクラッチバッファの配置に悩む必要はもうありません** — 0.8.0 で最後の外部スクラッチ引数を削除しました。

### F08-11 · 小見出し

スロットあたりの作業領域

### F08-12 · 表

|  |  |
|---|---|
| スペクトログラム | 約 360 KB（PSRAM 可） |
| 候補セット | 約 120 KB |
| LDPC BP スクラッチ | 約 12 KB |

### F08-13 · 小見出し

教訓：120 KB を返してもらった話

### F08-14 · 本文

初期実装は FT8 のトーン基底を Q15 の sin/cos 表として持っていました。 アセンブラの内積が本来の速度を出すには、この表が**高速な内部 SRAM**に居る必要がある — 30 KB × 2軸 × 2コア = **120 KB の内部 DRAM**。 そしてそれは、M5StickS3 の双方向 I2S DMA が必要とする領域そのものでした。QSO モードは起動に失敗しました。

### F08-15 · 本文

解決は Goertzel 再帰への置き換え。==スクラッチ 0、速度は据え置き、 精度はむしろ +0.16〜+0.63 dB 改善==。表を消したら全部よくなりました。

### F08-16 · 小見出し

動いている実機

### F08-17 · 箇条書き

- **M5Stack CoreS3**（ESP32-S3 LX7）— メインの受信機。USB Audio で無線機直結、4受信機を1イメージに。
- **M5StickS3**（ESP32-S3 LX7）— デモ／音響入力機。LCD UI、BLE CI-V、QSO ステートマシン。
- **M5Stack Core2**（ESP32 LX6）— LX6 側の検証機。USB ペリフェラルを持たないので WAV 再生での確認用。

### F08-18 · まとめ帯

呼び出し側が用意するのは FFT ひとつとアロケータだけ。スクラッチの置き場所で悩む必要はもうありません。

### F08-19 · フッタ

<span>mfsk-core — ハムフェア2026 ブース資料</span><span>08 / 10</span>

## F09 — 設計：Protocol トレイト

### F09-01 · 見出し

設計：Protocol トレイト

### F09-02 · 右肩の補足

コピペではなく型で、共通性を表現する

### F09-03 · 小見出し

プロトコル = ゼロサイズ型

### F09-04 · 本文

1つのモードは **実行時に1バイトも占めない型**として書かれ、その上に3つのトレイトを実装します。

### F09-05 · 表

|  |  |
|---|---|
| `ModulationParams` | トーン数、シンボルレート、Gray 写像、GFSK 整形 |
| `FrameLayout` | シンボル数、スロット長、同期ブロック配置 |
| `Protocol` | どの FEC と、どのメッセージ符号を使うか |

### F09-06 · 本文

共通パイプラインはすべて `P: Protocol` にジェネリックで、 コンパイル時に各モードへ**単相化**されます。==vtable なし、動的ディスパッチなし== — 抽象化のコストはコンパイル時間だけで、実行時にはゼロです。

### F09-07 · 注記

実測（2026-08-14）：`src/` の約3分の1、58.9k 行中 18.7k 行が `P: Protocol` にジェネリックなコードです。LDPC の BP/OSD カーネル、メッセージ符号、FFT/リサンプル/GFSK の DSP。

### F09-08 · 小見出し

共通（`engine`、全モードで1本）

### F09-09 · 本文

粗同期、精密同期、LLR 計算、等化、復調パイプライン本体、GFSK 合成

### F09-10 · 小見出し

モード固有（型の上の定数として宣言）

### F09-11 · 本文

トーン数・シンボルレート・Gray 写像・フレーム構成、そして どの FEC（LDPC / 畳み込み Fano / Reed-Solomon / GF(64) 上の QRA）と どのメッセージ幅（77 / 72 / 50 bit）を使うか

### F09-12 · 強調ボックス

FST4-60A の追加は、
<span style="color:var(--accent)">ZST 1つへの trait 実装 + レジストリ1行</span>でした。
<span style="font-weight:400;font-size:10.2pt">共通コードには一切触れていません。</span>

### F09-13 · 注記

Fortran のコードベースでは、モード間の「同じであり得たはずの部分」は ソースファイル間のコピー＆ペーストとして現れます。ここではそれをトレイトとして書き、 **何がモードごとに本当に違うのか**で分割しています。

### F09-14 · まとめ帯

モードを1つ足す作業が、共通コードを1行も触らずに終わる — それがこの設計の目的です。

### F09-15 · フッタ

<span>mfsk-core — ハムフェア2026 ブース資料</span><span>09 / 10</span>

## F10 — 一緒に作りませんか

### F10-01 · 見出し

一緒に作りませんか

### F10-02 · 右肩の補足

GPL-3.0-or-later

### F10-03 · 小見出し

手が足りていないところ

### F10-04 · 箇条書き

- **ブラウザで動く FT8 — WASM PWA のフロントエンド。**<br> <span class="note">ライブラリ側は wasm32 で動きます。UI とオーディオ入力を書く人がいません。</span>
- **Android / iOS アプリ。**<br> <span class="note">C ABI は用意済み。JNI の骨組みも置いてあります。</span>
- **ESP32 以外のマイコン。**<br> <span class="note">RP2350 + CMSIS-DSP、Cortex-M — FFT を1つ渡せば動くはずですが、まだ誰も試していません。</span>
- **CoreS3 の送信側。**<br> <span class="note">受信は動いています。残りは BLE CI-V、ADIF ログ、送信キーイング。</span>
- **JT65 の残差を詰める。**<br> <span class="note">自前コーパスでは差ゼロですが、実バイナリにはまだ届いていません。原因未特定 — 面白い問題です。</span>

### F10-05 · 小見出し

まず読むもの

### F10-06 · 表

|  |  |
|---|---|
| `README.md` | 全体像と Quick Start |
| `docs/reference/LIBRARY.md` | ホスト API の全体（日本語版あり） |
| `docs/reference/EMBEDDED.md` | 組み込み・固定小数点・C ABI（日本語版あり） |
| `docs/notes/BENCHMARKS.md` | 本日の数字の出どころすべて |
| `CONTRIBUTING.md` | テスト方針と WSJT-X 移植のルール |

### F10-07 · 注記

`docs/reference/` の文書はすべて日本語版（`.ja.md`）を持ちます。

### F10-08 · QRの説明

リポジトリ

### F10-09 · テキスト

**github.com/jl1nie/mfsk-core**
<span style="color:var(--muted);font-size:10.2pt">crates.io / docs.rs — `mfsk-core`（最新リリース v0.10.0、0.10.1 準備中）</span>
<span style="font-size:10.2pt">Issue も PR も歓迎します。
ブースで直接どうぞ — **JL1NIE**</span>

### F10-10 · 注記

本ライブラリは WSJT-X（K1JT Joe Taylor 氏および共同開発者の皆さん）の Rust 再実装です。 すべてのアルゴリズムの出典は WSJT-X にあり、基準実装はこれからも WSJT-X です。

### F10-11 · まとめ帯

電波を受けるコードが、Rust で、オープンに、誰の手元にもある状態を目指しています。

### F10-12 · フッタ

<span>mfsk-core — ハムフェア2026 ブース資料</span><span>10 / 10</span>

---

# 配布リーフレット（A4縦両面1枚・持ち帰り用）

## L01 — mfsk-core

### L01-01 · キッカー

WSJT-X MODES, IN PURE RUST

### L01-02 · 大見出し

mfsk-core

### L01-03 · キャッチ

WSJT-X のデコーダを、++まるごと Rust で++書き直しました。
デスクトップにも、ブラウザにも、スマホにも、++マイコンにも++。

### L01-04 · URL行

github.com/jl1nie/mfsk-core &nbsp;·&nbsp; crates.io/crates/mfsk-core &nbsp;·&nbsp; GPL-3.0-or-later &nbsp;·&nbsp; JL1NIE

### L01-05 · QRの説明

github.com/jl1nie/mfsk-core

### L01-06 · 見出し

これは何か

### L01-07 · 本文

FT8・FT4・FST4・WSPR・JT9・JT65・Q65・MSK144 — WSJT 系デジタルモードの復調と変調を、ひとつの純 Rust ライブラリにまとめたものです。 DSP、同期相関、LLR、誤り訂正（LDPC / 畳み込み / Reed-Solomon / QRA）、メッセージ符号を共通の土台として共有し、その上に各モードが載っています。

### L01-08 · 本文

**WSJT-X はこれからも基準実装です。**各ファイルは移植元の `lib/*.f90` / `lib/*.c` を明記しています。 このライブラリの存在理由は WSJT-X を置き換えることではなく、 ==Fortran + C + Qt のデスクトップアプリが行けない場所へ、同じアルゴリズムを持っていくこと==です。

### L01-09 · 見出し

ブースで動いているもの

### L01-10 · 図・主文

IC-705

### L01-11 · 図・副文

40 m
USB Audio Class

### L01-12 · 図・矢印ラベル

USB-C
1本

### L01-13 · 図・主文

M5Stack CoreS3

### L01-14 · 図・副文

ESP32-S3 LX7 240 MHz 2コア
8 MB PSRAM / 16 MB Flash

### L01-15 · 図・矢印ラベル

約1.2秒
スロット後

### L01-16 · 図・主文

LCD にコールサイン

### L01-17 · 図・副文

タッチでモード切替
WiFi / NTP / HTTP 設定

### L01-18 · 本文

この経路に PC は 1台も入っていません。復調はすべて CoreS3 の中で完結しています。

### L01-19 · 数値

6–8^ 局^

### L01-20 · 数値の説明

1スロット（15秒）あたりの FT8 デコード数
2026-08-23、IC-705 実機 40 m

### L01-21 · 数値

−24^ dB^

### L01-22 · 数値の説明

到達した最弱信号
（+8 〜 −24 dB）

### L01-23 · 数値

4^ 受信機^

### L01-24 · 数値の説明

1つのイメージに FT8 / WSPR / FST4 /
無線機不要のデモを同梱、タッチで切替

### L01-25 · 見出し

対応モード

### L01-26 · 表

| モード | スロット | 誤り訂正 | メッセージ |
|---|---|---|---|
| **FT8** | 15 s | LDPC(174, 91) + CRC-14 | 77 bit |
| **FT4** | 7.5 s | LDPC(174, 91) + CRC-14 | 77 bit |
| **FST4** -15/-30/-60A/-120/-300 | 15–300 s | LDPC(240, 101) + CRC-24 | 77 bit |
| **WSPR** | 120 s | 畳み込み r=½ K=32 + Fano | 50 bit |
| **JT9** | 60 s | 畳み込み r=½ K=32 + Fano | 72 bit |
| **JT65** | 60 s | Reed-Solomon(63, 12) GF(2⁶) | 72 bit |
| **Q65** -30A / -60A‥E | 30 / 60 s | QRA(15, 65) GF(2⁶) + CRC-12 | 77 bit |
| **MSK144** | 15 s | LDPC(128, 90) + CRC-13 | 77 bit |

### L01-27 · 見出し

感度は WSJT-X と同等、あるいはそれ以上

### L01-28 · 表

| モード | 実録音での再現率 | AWGN 50% 交差 |
|---|---|---|
| **FT8** | 8/8 · JTDX 18/18 | 約 −21.6 dB |
| **FT4** | 6/6 | 約 −16.9 dB |
| **WSPR** | 9/9 | 約 −31.5 dB |
| **JT9** | 7/7 | 約 −26.6 dB |
| **MSK144** | 3/3（SNR も一致） | 約 −5.2〜−5.8 dB |

### L01-29 · 小見出し

「再現率だけ」はベンチマークではない

### L01-30 · 本文

かつてこの表には再現率しか載っていませんでした。「WSPR 8/8、パリティ達成」と書かれていたデコーダは、 ==同時に8個の存在しないコールサインを出していました==。いまは誤デコードが常設の列で、上表はいずれも **0**。 数字はテストが assert しており、下回れば CI が落ちます。FST4・Q65 を含む全モードは `docs/notes/BENCHMARKS.md` に。

### L01-31 · フッタ

<span>mfsk-core — ハムフェア2026 / JL1NIE 友部 稔</span> <span>裏面：マルチプラットフォーム・組み込み・設計・参加のしかた &nbsp;▶</span>

## L02 — どこでも動く、という設計

### L02-01 · 大見出し

どこでも動く、という設計

### L02-02 · 右肩の補足

mfsk-core（裏面）

### L02-03 · 見出し

マルチプラットフォーム — ソースは1つ

### L02-04 · 表

| 行き先 | ターゲット | 状態と備考 |
|---|---|---|
| **デスクトップ / サーバ** | x86_64, aarch64 | 既定構成。rustfft + rayon。`cargo add mfsk-core` ですぐ使えます |
| **ブラウザ** | `wasm32-unknown-unknown` | rustfft の WASM SIMD。`+simd128` の明示を忘れずに（既定では付きません） |
| **Android / iOS** | NDK arm64-v8a ほか | NEON。C ABI 経由で、Fortran ランタイム不要 |
| **マイコン** | `no_std + alloc` — ESP32-S3 / RP2350 / Cortex-M | 実運用中。`fft-extern` + `fixed-point`、FFT は呼び出し側が注入 |
| **C / C++ から** | staticlib | `mfsk-ffi`（全モード）／`mfsk-ffi-ft8`（FT8 のみ）。ヘッダは cbindgen 生成 |

### L02-05 · 注記

**差し替えられるのは行き先だけではありません。**DSP と FEC 全体がスカラ型トレイトで抽象化され、ホストは f32、 マイコンは u16 スペクトログラム + i16 DFT + Q11i16 LLR + 整数 BP — コードは同じ一本です。FFT も外部注入。モードはすべて feature フラグです。

### L02-06 · 見出し

組み込み対応の中身

### L02-07 · 数値

約150^ KB^

### L02-08 · 数値の説明

FT8 復調に必要な使用可能 RAM
（FFT を呼び出し側が用意する場合）

### L02-09 · 数値

150–200^ KB^

### L02-10 · 数値の説明

ライブラリ分のフラッシュ使用量
（実測、アプリのグルー込み）

### L02-11 · 数値

0^ バイト^

### L02-12 · 数値の説明

内部 DRAM の常駐スクラッチ
／要求する extern シンボル

### L02-13 · 小見出し

呼び出し側が用意するもの

### L02-14 · 箇条書き

- **FFT の実装ひとつ** — `fft-extern` でファクトリ関数を1つ提供するだけ
- **アロケータ** — `no_std + alloc`。スロット単位の作業領域は PSRAM で構いません

### L02-15 · 注記

求めるのはこの2つだけで、スクラッチバッファの配置に悩む必要はありません。 スロットあたりの作業領域：スペクトログラム 約360 KB（PSRAM 可）、候補セット 約120 KB、LDPC BP スクラッチ 約12 KB。

### L02-16 · 小見出し

速度 — 同じ整数パイプラインで

### L02-17 · 表

|  |  |
|---|---|
| Ryzen 9 9900X（固定小数点） | 約 6 ms |
| **ESP32-S3 LX7 実機** | **1.19 s** |
| ESP32 Core2 LX6 実機 | 約 2.8 s |

### L02-18 · 注記

16局が混み合う WSJT-X 参照録音での実測。差はほぼ純粋な CPU 性能比で、**アルゴリズム上のオーバーヘッドはありません**。 WSPR も 1214秒 → 101.6秒まで詰め、9/9 を維持したまま 120秒スロットに収まりました。

### L02-19 · 見出し

設計 — Protocol トレイト

### L02-20 · 本文

1つのモードは **実行時に1バイトも占めない型**として書かれ、その上に `ModulationParams`（トーン数・シンボルレート・Gray 写像・GFSK 整形）、 `FrameLayout`（シンボル数・スロット長・同期配置）、 `Protocol`（どの FEC と、どのメッセージ符号を使うか）を実装します。 共通パイプラインはすべて `P: Protocol` にジェネリックで、コンパイル時に各モードへ単相化されます — ==vtable なし、動的ディスパッチなし==。抽象化のコストはコンパイル時間だけで、実行時にはゼロです。 実測で `src/` の約3分の1（58.9k 行中 18.7k 行）が全モード共通のジェネリックコードです。

### L02-21 · 本文

**これが効くということ：FST4-60A の追加は、ZST 1つへの trait 実装 + レジストリ1行でした**（共通コードには一切触れていません）。 Fortran では、モード間の「同じであり得たはずの部分」はソースファイル間のコピー＆ペーストとして現れます。ここではそれを型で表現しています。

### L02-22 · 見出し

一緒に作りませんか

### L02-23 · 箇条書き

- **ブラウザで動く FT8** — WASM PWA のフロントエンド（ライブラリ側は wasm32 で動きます）
- **Android / iOS アプリ** — C ABI は用意済み、JNI の骨組みもあります
- **ESP32 以外のマイコン** — RP2350、Cortex-M。FFT を1つ渡せば動くはずですが、まだ誰も試していません

### L02-24 · 箇条書き

- **CoreS3 の送信側** — 残るは BLE CI-V、ADIF ログ、送信キーイング
- **JT65 の残差** — 自前コーパスでは差ゼロ、実バイナリにはまだ届かず。原因未特定 — 面白い問題です

### L02-25 · QRの説明

リポジトリ

### L02-26 · CTA・見出し

Issue も PR も歓迎します。

### L02-27 · CTA・URL

github.com/jl1nie/mfsk-core

### L02-28 · CTA・補足

crates.io / docs.rs — `mfsk-core`（最新リリース v0.10.0、0.10.1 準備中）／ GPL-3.0-or-later ／ ブースで直接どうぞ — **JL1NIE 友部 稔**
まず読むもの：`README.md`／`LIBRARY.md`（ホスト API）／`EMBEDDED.md`（組み込み・C ABI）／`BENCHMARKS.md`（本紙の数字の出どころ）。`docs/reference/` は全文書に日本語版があります。

### L02-29 · フッタ

<span>本ライブラリは WSJT-X（K1JT Joe Taylor 氏ほか）の Rust 再実装です。すべてのアルゴリズムの出典は WSJT-X にあり、基準実装はこれからも WSJT-X です。</span>

